# Copy to frontend/.env for local overrides (gitignored).
#
# Local dev uses .env.development (localhost) automatically.
# Production uses Netlify env vars — see .env.production.example

VITE_GITHUB_USERNAME=your_github_username
# VITE_GITHUB_TOKEN=ghp_...
# VITE_BACKEND_URL=http://localhost:8000
