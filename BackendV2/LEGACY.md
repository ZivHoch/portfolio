# Legacy — BackendV2 (Vercel + Gemini)

This directory is **deprecated**. The active chat API lives in [`../backend/`](../backend/): FastAPI + RAG + Ollama.

It remains in the repo for reference only. Do not deploy new changes here.
